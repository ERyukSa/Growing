<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.eryuksa.growing.todo.TodoViewModel" />
        <variable
            name="todo"
            type="com.eryuksa.growing.todo.TodoItem.Todo" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <CheckBox
            android:id="@+id/checkBox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:onCheckedChangeListener="@{(checkBox, checked) -> viewModel.onTodoStatusChanged(todo, checked)}"
            android:layout_gravity="top" />

        <TextView
            android:id="@+id/contents"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@{todo.contents}"
            app:strikeThrough="@{todo.done}"
            tools:text="앱 개발하기" />

        <androidx.appcompat.widget.AppCompatImageButton
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_round_delete_outline_24"
            android:onClick="@{() -> viewModel.remove(todo)}"/>
    </LinearLayout>
</layout>